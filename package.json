{
  "name": "exam_project_ux",
  "version": "1.0.0",
  "description": "A raw, semantic HTML and pure CSS clothing e-commerce site with no styling frameworks.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch db.json --port 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
